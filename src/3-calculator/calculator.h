/* calculator.h
/
/  The following program should function as a basic calculator; it should ask the user to input what type of 
/  arithmetic operation he would like, and then ask for the numbers on which the operation should be performed. 
/  The calculator should then give the output of the operation.
*/

#ifndef CALCULATOR
#define CALCULATOR

void calculator();
float multiply (float, float);
float add (float, float);
float subtract(float, float);
float divide(float, float);

#endif